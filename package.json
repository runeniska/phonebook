{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Phonebook backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ~/fullstackopen2021/osa3/phonebook-frontend && npm run build --prod && cp -r build ~/phonebook",
    "deploy": "git push heroku master"
  },
  "author": "Rune Niska",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.12"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.5",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  }
}
